name: Matrix

on: [ push ]

jobs:
  node-version:
    runs-on: ubuntu-16.04
    steps:
      - name: Log Node Version
        run: node -v
      - uses: actions/setup-node@v1
        with:
          node-version: 6
      - name: Log Node Version
        run: node -v
